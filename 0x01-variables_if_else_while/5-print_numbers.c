#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always to 0 (success)
 */
int main(void)
{
	int i;

	i = 0;
		 while (i < 10)
		{
			printf("%d", i);
			i++;
		}
	printf("\n");
	return (0);
}
