#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always to 0 (success)
 */
int main(void)
{
	int i;
		for (i = '0'; i <= 9)
		{
			printf("%d", i);
			i++;
		}
	printf("\n");
	return (0);
}
